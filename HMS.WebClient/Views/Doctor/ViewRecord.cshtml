@model HMS.Shared.DTOs.MedicalRecordDto

@{
    ViewData["Title"] = "Medical Record Details";
}

<div class="container mt-4">
    <h2>@ViewData["Title"]</h2>

    <div class="card">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">Date</dt>

                <dt class="col-sm-3">Patient ID</dt>
                <dd class="col-sm-9">@Model.PatientId</dd>

                <dt class="col-sm-3">Doctor ID</dt>
                <dd class="col-sm-9">@Model.DoctorId</dd>

                <dt class="col-sm-3">Procedure ID</dt>
                <dd class="col-sm-9">@Model.ProcedureId</dd>

                <dt class="col-sm-3">Diagnosis</dt>
                <dd class="col-sm-9">@Model.Diagnosis</dd>
            </dl>

            <div class="mt-4">
                <a asp-action="MedicalHistory" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to Medical History
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://kit.fontawesome.com/your-font-awesome-kit.js"></script>
} 