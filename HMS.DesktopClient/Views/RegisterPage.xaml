<Window
    x:Class="HMS.DesktopClient.Views.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HMS.DesktopClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Register Patient">

    <ScrollViewer>
        <StackPanel Spacing="10" Padding="30">
            <TextBlock Text="Register as Patient" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBox x:Name="EmailBox" PlaceholderText="Email"/>
            <PasswordBox x:Name="PasswordBox" PlaceholderText="Password"/>
            <TextBox x:Name="NameBox" PlaceholderText="Full Name" MaxLength="50"/>
            <TextBox x:Name="CnpBox" PlaceholderText="CNP" MaxLength="13"/>
            <TextBox x:Name="PhoneNumberBox" PlaceholderText="Phone Number" MaxLength="10"/>
            <ComboBox x:Name="BloodTypeBox" PlaceholderText="Blood Type">
                <ComboBoxItem Content="A+"/>
                <ComboBoxItem Content="A-"/>
                <ComboBoxItem Content="B+"/>
                <ComboBoxItem Content="B-"/>
                <ComboBoxItem Content="AB+"/>
                <ComboBoxItem Content="AB-"/>
                <ComboBoxItem Content="O+"/>
                <ComboBoxItem Content="O-"/>
            </ComboBox>
            <TextBox x:Name="EmergencyContactBox" PlaceholderText="Emergency Contact" MaxLength="10"/>
            <TextBox x:Name="AllergiesBox" PlaceholderText="Allergies (optional)"/>
            <TextBox x:Name="WeightBox" PlaceholderText="Weight (kg)" MaxLength="5"/>
            <TextBox x:Name="HeightBox" PlaceholderText="Height (cm)" MaxLength="5"/>
            <DatePicker x:Name="BirthDatePicker" Header="Birth Date"/>
            <TextBox x:Name="AddressBox" PlaceholderText="Address"/>

            <TextBlock x:Name="ErrorTextBlock" Foreground="Red" Visibility="Collapsed"/>

            <Button Content="Register" Click="RegisterButton_Click"/>
        </StackPanel>
    </ScrollViewer>
</Window>
