<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HMS.DesktopClient.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HMS.DesktopClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HomePage">

    <Grid x:Name="RootGrid" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding Name}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Email:" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding Email}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Role:" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding Role}" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="CNP:" Grid.Row="3" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding CNP}" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Phone Number:" Grid.Row="4" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding PhoneNumber}" Grid.Row="4" Grid.Column="1" Margin="5"/>

        <TextBlock Text="User ID:" Grid.Row="5" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding Id}" Grid.Row="5" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Created At:" Grid.Row="6" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding CreatedAt}" Grid.Row="6" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Token:" Grid.Row="7" Grid.Column="0" Margin="0,5"/>
        <TextBlock Text="{Binding Token}" Grid.Row="7" Grid.Column="1" Margin="5"/>
    </Grid>

</Window>
