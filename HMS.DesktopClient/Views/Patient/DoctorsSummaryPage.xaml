<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HMS.DesktopClient.Views.Patient.DoctorsSummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HMS.DesktopClient.Views.Patient"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <TextBlock Text="Doctors" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        <ListView x:Name="DoctorsListView" Margin="0,40,0,0">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="dto:DoctorListItemDto" xmlns:dto="using:HMS.Shared.DTOs.Doctor">
                    <StackPanel Orientation="Horizontal" Spacing="20">
                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" Width="200"/>
                        <TextBlock Text="{x:Bind DepartmentName}" Width="200"/>
                        <TextBlock Text="{x:Bind YearsOfExperience}" Width="100" TextAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
